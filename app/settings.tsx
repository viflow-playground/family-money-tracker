import { Alert, StyleSheet, Text, TextInput, TouchableOpacity, View } from "react-native";
import { useState } from "react";
import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useRouter } from "expo-router";
import { useFamily } from "@/components/family-provider";
import { ScreenContainer } from "@/components/screen-container";
import { haptic } from "@/lib/haptics";
type Editor = { kind: "family" | "currency" | "member"; memberId?: string; label: string } | null;
export default function SettingsScreen() { const router = useRouter(); const { data, resetFamily, renameMember, saveFamilyProfile, restartIntro } = useFamily(); const [editor, setEditor] = useState<Editor>(null); const [draft, setDraft] = useState(""); const open = (next: Editor, value: string) => { haptic.light(); setEditor(next); setDraft(value); }; const save = () => { const value = draft.trim(); if (!value) return Alert.alert("Add a value", "Enter a value before saving."); if (editor?.kind === "member") renameMember(editor.memberId as string, value); else saveFamilyProfile({ name: editor?.kind === "family" ? value : data.familyProfile.name, currencySymbol: editor?.kind === "currency" ? value : data.familyProfile.currencySymbol }); haptic.success(); setEditor(null); }; const reset = () => Alert.alert("Reset this device’s family data?", "This removes locally recorded choices and returns the example family record. This cannot be undone.", [{ text: "Keep my data", style: "cancel" }, { text: "Reset local data", style: "destructive", onPress: () => { resetFamily(); haptic.success(); router.back(); } }]); const row = (icon: keyof typeof MaterialIcons.glyphMap, label: string, value: string, action: () => void, color = "#176B73") => <TouchableOpacity style={styles.row} activeOpacity={0.75} onPress={action}><View style={[styles.rowIcon, { backgroundColor: `${color}18` }]}><MaterialIcons name={icon} size={19} color={color} /></View><View style={styles.rowCopy}><Text style={styles.rowLabel}>{label}</Text><Text style={styles.rowValue}>{value}</Text></View><Text style={[styles.editText, { color }]}>Edit</Text><MaterialIcons name="chevron-right" size={20} color="#89949A" /></TouchableOpacity>; return <ScreenContainer><View style={styles.content}><Text style={styles.eyebrow}>ON THIS DEVICE</Text><Text style={styles.title}>{data.familyProfile.name}</Text><Text style={styles.subtitle}>Make this shared space feel like your own family.</Text><View style={styles.card}><Text style={styles.cardLabel}>FAMILY PROFILE</Text>{row("home", "Family name", data.familyProfile.name, () => open({ kind: "family", label: "FAMILY NAME" }, data.familyProfile.name))}<View style={styles.divider} />{row("payments", "Default currency symbol", data.familyProfile.currencySymbol, () => open({ kind: "currency", label: "CURRENCY SYMBOL" }, data.familyProfile.currencySymbol), "#9A6B18")}</View><View style={styles.card}><Text style={styles.cardLabel}>FAMILY MEMBERS</Text>{data.members.map((member, index) => <View key={member.id}>{row(member.role === "parent" ? "manage-accounts" : "face", member.role === "parent" ? "Parent name" : "Child name", member.name, () => open({ kind: "member", memberId: member.id, label: member.role === "parent" ? "PARENT NAME" : "CHILD NAME" }, member.name), member.color)}{index < data.members.length - 1 && <View style={styles.divider} />}</View>)}</View>{editor && <View style={styles.editor}><Text style={styles.editorLabel}>{editor.label}</Text><TextInput value={draft} onChangeText={setDraft} maxLength={editor.kind === "currency" ? 6 : 32} autoFocus returnKeyType="done" onSubmitEditing={save} style={styles.input} accessibilityLabel={editor.label} /><Text style={styles.helper}>{editor.kind === "currency" ? "Examples: HK$, $, ¥, € or USD$" : "This name appears across the family app."}</Text><View style={styles.editorActions}><TouchableOpacity style={styles.cancelButton} activeOpacity={0.75} onPress={() => setEditor(null)}><Text style={styles.cancelText}>Cancel</Text></TouchableOpacity><TouchableOpacity style={styles.saveButton} activeOpacity={0.82} onPress={save}><Text style={styles.saveText}>Save changes</Text></TouchableOpacity></View></View>}<TouchableOpacity style={styles.summaryButton} activeOpacity={0.78} onPress={() => router.push("/monthly-summary")}><View style={styles.summaryIcon}><MaterialIcons name="insights" size={20} color="#6C4A9B" /></View><View style={styles.summaryCopy}><Text style={styles.summaryTitle}>Monthly family progress</Text><Text style={styles.summaryText}>See savings, earnings, work, and money choices together.</Text></View><MaterialIcons name="chevron-right" size={22} color="#6C4A9B" /></TouchableOpacity><TouchableOpacity style={styles.introButton} activeOpacity={0.76} onPress={() => { haptic.light(); restartIntro(); router.back(); }}><MaterialIcons name="auto-stories" size={19} color="#176B73" /><Text style={styles.introText}>Replay the quick app introduction</Text></TouchableOpacity><TouchableOpacity style={styles.resetButton} activeOpacity={0.75} onPress={reset}><MaterialIcons name="restart-alt" size={19} color="#B44733" /><Text style={styles.resetText}>Reset local family data</Text></TouchableOpacity></View></ScreenContainer>; }
const styles = StyleSheet.create({ content: { flex: 1, padding: 20, paddingTop: 16 }, eyebrow: { color: "#176B73", fontSize: 10, letterSpacing: 1.2, fontWeight: "800" }, title: { color: "#1D2C3A", fontSize: 28, fontWeight: "800", marginTop: 3 }, subtitle: { color: "#65737C", fontSize: 13, lineHeight: 19, marginTop: 6 }, card: { marginTop: 19, backgroundColor: "#FFFFFF", borderWidth: 1, borderColor: "#E6E4DD", borderRadius: 22, padding: 15 }, cardLabel: { color: "#8A959B", fontSize: 10, letterSpacing: 0.9, fontWeight: "900", marginBottom: 8 }, row: { flexDirection: "row", alignItems: "center", gap: 10, minHeight: 57 }, rowIcon: { width: 34, height: 34, alignItems: "center", justifyContent: "center", borderRadius: 12 }, rowCopy: { flex: 1 }, rowLabel: { color: "#78848B", fontSize: 10, fontWeight: "700" }, rowValue: { color: "#263640", fontSize: 14, fontWeight: "800", marginTop: 2 }, editText: { fontSize: 11, fontWeight: "800" }, divider: { height: 1, backgroundColor: "#F0EFEB", marginLeft: 44 }, editor: { backgroundColor: "#E4F1F0", borderRadius: 18, padding: 14, marginTop: 14 }, editorLabel: { color: "#176B73", fontSize: 10, letterSpacing: 0.8, fontWeight: "900" }, input: { backgroundColor: "#FFFFFF", borderWidth: 1, borderColor: "#B9D8D5", color: "#1D2C3A", borderRadius: 12, paddingHorizontal: 12, paddingVertical: 11, fontSize: 14, fontWeight: "700", marginTop: 9 }, helper: { color: "#527477", fontSize: 11, lineHeight: 16, marginTop: 7 }, editorActions: { flexDirection: "row", justifyContent: "flex-end", gap: 9, marginTop: 9 }, cancelButton: { paddingVertical: 9, paddingHorizontal: 11 }, cancelText: { color: "#65737C", fontSize: 12, fontWeight: "800" }, saveButton: { backgroundColor: "#176B73", paddingVertical: 9, paddingHorizontal: 13, borderRadius: 10 }, saveText: { color: "#FFFFFF", fontSize: 12, fontWeight: "800" }, summaryButton: { alignItems: "center", flexDirection: "row", gap: 10, borderRadius: 18, backgroundColor: "#F1ECFA", padding: 14, marginTop: 18 }, summaryIcon: { width: 35, height: 35, alignItems: "center", justifyContent: "center", borderRadius: 12, backgroundColor: "#FFFFFF" }, summaryCopy: { flex: 1 }, summaryTitle: { color: "#6C4A9B", fontSize: 13, fontWeight: "800" }, summaryText: { color: "#6F6381", fontSize: 11, lineHeight: 16, marginTop: 3 }, introButton: { alignItems: "center", justifyContent: "center", flexDirection: "row", gap: 7, borderRadius: 15, backgroundColor: "#E4F1F0", paddingVertical: 14, marginTop: 14 }, introText: { color: "#176B73", fontSize: 13, fontWeight: "800" }, resetButton: { alignItems: "center", justifyContent: "center", flexDirection: "row", gap: 7, borderRadius: 15, backgroundColor: "#FCEBE7", paddingVertical: 14, marginTop: 14 }, resetText: { color: "#B44733", fontSize: 13, fontWeight: "800" } });
